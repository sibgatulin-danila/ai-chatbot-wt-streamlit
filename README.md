# Local AI Chatbot

Простой чатбот с локальной LLM через Streamlit и Ollama. Общайтесь с ИИ без отправки данных в облако!

## 🚀 Быстрый старт

1. **Установите Ollama**  
   ```bash
   curl -fsSL https://ollama.com/install.sh | sh
   ```

2. **Запустите Ollama**  
   ```bash
   ollama serve
   ```

3. **Скачайте модель** (выберите одну)  
   ```bash
   ollama pull phi3        # рекомендуется (лёгкая и быстрая)
   ollama pull mistral     # мощная модель
   ollama pull llama3      # от Meta
   ```

4. **Установите зависимости**  
   ```bash
   pip install streamlit ollama
   ```

5. **Запустите приложение**  
   ```bash
   streamlit run app.py
   ```

Готово! Откройте `http://localhost:8501` и начните чат.

## 📁 Структура проекта

```
local-ai-chatbot/
├── app.py              # основное приложение
├── requirements.txt    # зависимости
└── README.md           # этот файл
```

## ⚙️ Настройка

По умолчанию используется модель `phi3`. Чтобы изменить модель:

1. Выберите ее в меню приложения через выпадающий список:
2. Убедитесь, что модель скачана через `ollama pull <model_name>`

## 💡 Требования

- Python 3.8+
- Ollama (установлен и запущен)
- Достаточно оперативной памяти для выбранной модели:
  - `phi3`: ~2 ГБ RAM
  - `mistral`/`llama3`: ~8 ГБ RAM
